const name = "Alice";
const age = 30;

// Old way
console.log("Hi " + name + ". You are " + age + " years old.");

// Template literals:
console.log(`Hi, ${name}. You are ${age} years old.`);
// Multi-line:
console.log(`line one\nline two`);